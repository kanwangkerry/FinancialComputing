package hw1;

public interface IPositionIter {
	/**
	 * This method returns the next Position in the bag and null if we already
	 * iterated over all the position
	 **/
	public IPosition getNextPosition();
}